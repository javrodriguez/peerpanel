# The private evaluator record — never part of this repo (it holds its own nested git repo)
gauntlet/

# Fetched demo-scale corpus documents (pinned by corpus/demo.manifest.json; fetch with `make corpus`)
corpus/demo/

# Derived indexes and run artifacts — bulky and regenerable from the committed
# extraction/summary caches. The MEASUREMENTS they produce are NOT ignored:
# they live in results/, which is tracked, because a number nobody can see is
# not a published result.
artifacts/

# Secrets — never committed anywhere
.env

# Python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# --- Glitch publish boundary (added by workspace-git) ---
.planning/
STATE.md
CLAUDE.md
AGENTS.md
