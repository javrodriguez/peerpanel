# Prepared diffs — written, never applied

Two one-line cross-links that point at this repository from places Javier already owns.
They are staged here and applied **only** inside the flip's yes (member gate 2), because both targets are public and this repository is private: applied earlier, each would publish a dead link.

Neither diff is applied by any command in this repository.
Applying them is a deliberate act taken at the flip, in the target repositories, by their owner.

| file | target | cut against |
|---|---|---|
| `gars-readme.diff` | `javrodriguez/genomics-agentic-research-system`, `README.md` | head `c0c5f47f1aed6839e2bdc6e6ac50e2a668e7825f` (read 2026-09-04) |
| `github-profile.diff` | `javrodriguez/javrodriguez`, `README.md` (the profile page) | head read 2026-09-04 |

**The re-show rule.** A yes binds the diff **as shown**. If either target's README has moved since the diff was shown, that diff is re-cut and re-shown, and the earlier yes lapses for that line. The head each was cut against is recorded above so the check is a one-line comparison rather than a memory.

**Whose words these are.** Every sentence in either diff has Javier as its subject or speaks in his voice on his own pages, so the wording is his to approve or replace verbatim (member gate 4). What is drafted here is a starting point, not a claim already made.
