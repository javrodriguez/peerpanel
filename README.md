# PeerPanel

Multi-agent scientific review with GraphRAG retrieval over a pinned open-access corpus.

**Status: under construction.** This repo is being built in verified checkpoints; nothing is
claimed here until the measurement that backs it is committed beside it. The architecture
README, results tables and demo land as their checkpoints complete.

- License: MIT for the code; documents under `corpus/` and `manuscripts/` keep their own
  CC licenses, recorded per-document in CITATIONS.md.
- `make test` / `make lint` run today; the rest of the Makefile names the build plan.
